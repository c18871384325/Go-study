package main

import (
	"fmt"
)

func main() {
	/*
	变量的名称（name）
	标识符：程序种定义的名字：变量名，常量名，函数名，自定义类型，接口，等；
	
	组成：
		1.标识符必须满足非空的 unicoe编码字符串，数字，下划线组成
		2.必须满足不能以数字开头
		3.不能与Go语言的关键字冲突
	
	建议：（非必须满足）
		1.Ascill编码（a-z,A-Z），数字，下划线
		2.变量使用驼峰式
			多个引文字母 my_name myName（驼峰式）
		3.与Go内置的标识符不要冲突（string...）
	
	说明：标识符区分大小写
	*/

	
}